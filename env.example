# Supabase Database Configuration
# Get these values from your Supabase project dashboard
# For development: Create a separate Supabase project for dev environment
# For production: Use your production Supabase project and set as GitHub secrets
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
# Service role key for admin operations (bypasses RLS)
VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-account
VITE_SUPERBASE_API_KEY=your-api-key
VITE_SUPABASE_DIRECT_CONNECT_PASSWORD=your-direct-connect-password

# Admin Panel Configuration
# Set a secure password for accessing the admin panel
# This should be stored as a GitHub secret in production
VITE_ADMIN_PASSWORD=your-secure-admin-password-here

# reCAPTCHA Configuration (Optional)
# Get your site key from Google reCAPTCHA Console (https://www.google.com/recaptcha/admin)
# This is the public site key that will be used in the frontend
VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here

# Database Retry Configuration (Optional)
# Time to wait between database connection retry attempts (in milliseconds)
# Default: 60000 (1 minute) - recommended to avoid spamming the database
VITE_DATABASE_RETRY_DELAY=60000

# Maximum number of database connection retry attempts
# Default: 3 attempts
VITE_DATABASE_MAX_RETRIES=3

# Image Upload Configuration (Optional)
#
# LOCAL DEVELOPMENT:
# Copy this file to .env and adjust values as needed for local testing
#
# NETLIFY PRODUCTION:
# Set these in your Netlify dashboard: Site settings > Environment variables > Add variable
# Or via Netlify CLI: netlify env:set VITE_IMAGE_MAX_FILES 10
# Or uncomment in netlify.toml build environment section

# Maximum number of images per field/question
# Local default: 10 images (adjust for local testing)
# Production suggestion: 5-10 images (adjust based on storage costs)
VITE_IMAGE_MAX_FILES=10

# Maximum file size per image in MB
# Local default: 10 MB per file (good for testing)
# Production suggestion: 2-5 MB (adjust based on storage costs and performance)
VITE_IMAGE_MAX_FILE_SIZE_MB=10

# Allowed image file types (comma-separated)
# Default: JPEG, PNG, GIF, WebP formats
# Production suggestion: Consider removing GIF if not needed to save storage
VITE_IMAGE_ALLOWED_TYPES=image/jpeg,image/jpg,image/png,image/gif,image/webp